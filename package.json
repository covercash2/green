{
  "name": "green-js",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "deno bundle --platform=browser --minify --outdir assets/js src/js/auth-login.ts src/js/auth-register.ts src/js/mqtt.ts",
    "test": "deno test --no-check test/js/*.test.ts",
    "coverage": "deno test --no-check --coverage=.deno-coverage test/js/*.test.ts"
  }
}
